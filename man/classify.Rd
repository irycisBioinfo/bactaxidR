% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/classify.R
\name{classify}
\alias{classify}
\title{Classify genomes using BacTaxID}
\usage{
classify(
  fasta_files,
  genus = NULL,
  db_path = NULL,
  db_dir = tools::R_user_dir("bactaxidR", which = "data"),
  bin_path = NULL,
  bin_dir = tools::R_user_dir("bactaxidR", which = "data"),
  verbose = TRUE
)
}
\arguments{
\item{fasta_files}{Character vector of paths to input FASTA files to classify.}

\item{genus}{Character (optional). Bacterial genus of the database to use. If specified,
the database `<genus>.db` will be searched under `db_dir` or downloaded if missing.}

\item{db_path}{Character (optional). Direct path to a DuckDB database file (`.db`). If specified,
takes precedence over `genus`.}

\item{db_dir}{Character. Directory to search for or download reference databases.}

\item{bin_path}{Character (optional). Direct path to the BacTaxID executable. If NULL, searches under `bin_dir`.}

\item{bin_dir}{Character. Directory to search for or download the BacTaxID executable.}

\item{verbose}{Logical. If `TRUE`, prints progress and stdout/stderr of the BacTaxID executable.}
}
\value{
A `data.frame` of class `c("btx_cls", "data.frame")` containing classification results with columns
  such as `query_id`, `query_signature`, `best_hit_id`, `best_hit_signature`, `similarity_score`,
  `levels_reached`, `final_code`, and `best_hit_code`.
}
\description{
Performs taxonomic classification on a set of input genome files (in FASTA format)
against a genus-specific DuckDB reference database.
}
